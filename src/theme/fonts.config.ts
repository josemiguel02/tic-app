import { theme } from '@chakra-ui/react'

export const fonts = {
  heading: `'Poppins', ${theme.fonts?.heading}`,
  body: `'Poppins', ${theme.fonts?.body}`,
  typingFont: `'Inconsolata', ${theme.fonts?.mono}`
}
